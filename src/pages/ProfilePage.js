export function ProfilePage(props){
    const {p, setp} = props;
    // if(p.role === "DRIVER"){
    //     return(
    //         <DriverMenu p={p} setp={setp}/>
    //     );
    // }
    return
    // (
    //   <OfficeMenu p={p} setp={setp}/>
    // )
    ;
}